declare const DB_HOST: string;
declare const DB_PORT: number;
declare const DB_USERNAME: string;
declare const DB_PASSWORD: string;
declare const DB_NAME: string;
export { DB_HOST, DB_NAME, DB_PASSWORD, DB_PORT, DB_USERNAME };
